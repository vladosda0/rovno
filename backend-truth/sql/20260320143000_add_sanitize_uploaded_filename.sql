-- Generated file. Read-only. Regenerate from rovno-db.
-- Do not edit this file directly.
-- Contract precedence: mirrored SQL is the exact audit source within backend-truth.
-- Source: supabase/migrations/20260320143000_add_sanitize_uploaded_filename.sql
-- Regenerate with: node scripts/generate-backend-truth.mjs
create or replace function public.sanitize_uploaded_filename(input text)
returns text
language plpgsql
immutable
as $$
declare
  v text;
begin
  v := coalesce(trim(input), 'file');
  v := regexp_replace(v, '\s+', '-', 'g');
  v := regexp_replace(v, '[^a-zA-Z0-9._-]', '', 'g');

  if v = '' then
    v := 'file';
  end if;

  return lower(v);
end;
$$;
