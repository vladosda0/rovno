-- Generated file. Read-only. Regenerate from rovno-db.
-- Do not edit this file directly.
-- Contract precedence: mirrored SQL is the exact audit source within backend-truth.
-- Source: supabase/migrations/20260605130000_payment_intents_consent.sql
-- Regenerate with: node scripts/generate-backend-truth.mjs
-- Расширяет payment_intents записью факта согласия на регулярные (рекуррентные)
-- списания. Требование Т-Банка для подключения автосписаний: перед оплатой
-- покупатель самостоятельно проставляет чек-бокс согласия на последующие
-- списания. tbank-init-payment фиксирует этот факт на intent'е в момент создания
-- (Init), что служит доказательством согласия при оспаривании платежа (chargeback).
--
--   - consent_accepted_at: момент проставления согласия (= момент Init). NULL для
--     intent'ов, созданных до внедрения требования.
--   - consent_version: версия текста согласия (например 'recurring-2026-06-05'),
--     чтобы при изменении формулировки можно было доказать, с какой именно
--     редакцией согласился покупатель. NULL, если версия не передавалась.
--
-- Оба поля nullable: бэкфилл не нужен, существующие in-flight intent'ы остаются
-- валидными. Индекс не добавляется — выборок по согласию нет.
--
-- COMMENT ON и `if not exists` не используются — backend-truth парсер их не поддерживает.

alter table public.payment_intents
  add column consent_accepted_at timestamptz,
  add column consent_version text;
