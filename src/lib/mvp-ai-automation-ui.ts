/**
 * MVP: hide AI automation mode pickers (sidebar, onboarding, settings).
 * When false, behaviour defaults to manual control and related UI is not shown.
 */
export const MVP_SHOW_AI_AUTOMATION_MODE_UI = false;
