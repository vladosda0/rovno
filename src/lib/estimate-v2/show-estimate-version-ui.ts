/**
 * When false, user-visible estimate version labels (e.g. "Estimate v1") are not shown.
 * Versioning data paths stay in place for a future release; flip to true to restore labels.
 */
export const SHOW_ESTIMATE_VERSION_UI = false;
